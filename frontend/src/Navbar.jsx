import React from 'react';
import './Navbar.css'; // Import the CSS file for styling

const Navbar = () => {
  return (
    <nav className="navbar">
      <h1 class="navbar-title">
       <span class="half-black">med</span><span class="half-green">Life</span>
      </h1>
    </nav>
  );
};

export default Navbar;
